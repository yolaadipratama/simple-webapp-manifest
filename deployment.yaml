apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-webapp
  namespace: webapp
spec:
  selector:
    matchLabels:
      app: simple-webapp
  replicas: 2
  template:
    metadata:
      labels:
        app: simple-webapp
    spec:
      containers:
        - name: simple-webapp
          image: docker.io/yolaadipratama/webapp:2212210801
          ports:
            - containerPort: 80

